import React from 'react';
import Loader from '../components/Loader';

const Connect = () => {
    return (
        <div>
            <Loader/>
            Connect
        </div>
    );
};

export default Connect;